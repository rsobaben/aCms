import { NextFunction, Request, Response } from "express";

export class BaseRoute {

}


/*

interface class Routes{
  public static create (route: express.Router)
}

// sempre adicionar um folder?!
pegar os routes/ * /.ts

route  = express.Route();

let routerClass = = Loader.route(name);

routerClass.create(route)


*/